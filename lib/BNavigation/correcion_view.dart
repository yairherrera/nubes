import 'package:flutter/material.dart';

class correcion_view extends StatelessWidget {
  const correcion_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vista de correcion'),
    );
  }
}
