import 'package:flutter/material.dart';

class user_view extends StatelessWidget {
  const user_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vista de usuario'),
    );
  }
}
